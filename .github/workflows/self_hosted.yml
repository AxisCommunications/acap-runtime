---
name: temp testing self self-hosted

on: [push]

jobs:
  job1:
    name: The first job
    runs-on: self-hosted
    steps:
      - run: echo "🎉 This is a self-hosted job triggered by a ${{ github.event_name }} event."
      - run: docker pull axisecp/acap-runtime:armv7hf-test
      - run: docker image rm axisecp/acap-runtime:armv7hf-test
  build:
    needs: job1

    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 This is a github hosted job."
